# TASK-062 — M11.3C Real Operational Escape Harness Blueprint

STATUS: LOCKED
MILESTONE: M11.3C — REAL OPERATIONAL ESCAPE PROOF
BASELINE_MAIN_SHA: d6f51f14188ffc56fd06bc887b68d9cad550c9e0

## 1. Purpose

Implement the final reusable runtime harness needed to perform exactly one Human-authorized real MiniMax paid-API Brain escape after this implementation itself has been reviewed and merged.

TASK-062 is an IMPLEMENTATION-ONLY / NO-SPEND task. The executor may code and test the harness, but must never perform the real paid provider call during RUN/FIX, must never consume a real paid grant, and must never use a real API credential.

The actual M11.3C live proof is a separate post-merge Human operation using the merged harness and a fresh bounded Human paid-API grant.

## 2. Authority Separation

```text
Codex/Antigravity task authorization
    = authority to edit/test TASK-062 scope only
    != paid API spend authorization

Human paid-api grant
    = one bounded Brain spend authorization
    != Executor authorization
    != merge authorization

M11.3C provider response
    = advisory Brain proposal only
    != worktree authority
    != review PASS
```

M11 v1 remains Brain-side only. A paid-API Executor transport remains forbidden.

## 3. Executor Surface

TASK-062 may be executed by either Unified Worker surface, but Codex is explicitly supported and recommended for this task:

```text
Codex:       $aios-worker RUN TASK-062 -> --adapter codex -> executor_id=codex
Antigravity: /aios-worker RUN TASK-062 -> --adapter antigravity -> executor_id=antigravity
```

No silent reroute, cross-surface inference, executor failover, or second executor.

## 4. Existing Trusted Components — Reuse, Do Not Redesign

The harness MUST compose the already-merged contracts rather than duplicate their authority:

```text
M10 dispatch:
  BrainDispatchRequest / BrainDispatchCandidate / dispatch_brain

M11.1 / M11.2A-B:
  PaidApiGrant / AtomicPaidApiGrantStore

M11.2C:
  execute_paid_api_brain_escape
  consume-before-call
  one gateway invocation / no retry

M11.2C.1-C.2:
  ProviderInputCountEvidence
  MiniMaxM3LocalProviderInputCounter
  trusted local counter identity
  pinned MiniMax tokenizer/template bundle

M11.3A:
  PaidApiOperationalProofReceipt
  build_paid_api_operational_proof

M11.3B:
  MiniMaxM3ProofLock
  PaidApiProofPreflightReceipt
  canonical .ai/ proof-lock path
  offline preflight / provenance lock

External Brain:
  ModelGateway
  MiniMaxOpenAIProvider
  JsonlUsageLedger
```

TASK-062 MUST NOT weaken or reinterpret any of these contracts.

## 5. New Runtime Harness

Create a reusable module `src/aios_bridge/paid_api_real_escape.py` and Bridge command `paid-proof-execute`.

The module must use dependency injection so unit/integration tests can exercise the complete control flow with fake/local transports and zero network. Production construction of the real MiniMax provider is confined to the Bridge command after every pre-call gate passes.

### Command surface

```text
python bridge.py paid-proof-execute <task_id>
  --grant-id <grant-id>
  --proof-lock-path <canonical .ai/ path>
  --proof-lock-blob-sha <exact Git blob SHA>
  --subscription-brain-id <canonical actor id>
  --subscription-capacity-fingerprint <exact 64-hex>
  --paid-capacity-fingerprint <exact 64-hex>
```

No CLI override for:

```text
api key / token / auth header
provider id
model id
endpoint/base URL/path
asset directory
ledger absolute path
workspace id
max calls
allow_paid_api
retry
executor transport
```

Provider/model/endpoint/credential source come only from the Git-bound `MiniMaxM3ProofLock` and exact Human grant.

## 6. Exact Live-Proof Sequence — R0 → R10

The production command MUST fail closed in this order.

### R0 — Local current-main gate

Require:

```text
worktree clean
current branch = main
HEAD == local main == origin/main
```

Use already-present local refs. Do not perform hidden `git fetch` inside the paid execution command.

### R1 — Canonical proof lock

Resolve only the exact local `origin/ai-control` tracking ref and require:

```text
canonical normalized .ai/... proof_lock_path
resolved Git blob == --proof-lock-blob-sha
strict MiniMaxM3ProofLock parse
provider = minimax
model = MiniMax-M3
endpoint exact allowlist
credential source = MINIMAX_API_KEY
pinned source revision and dependency versions
```

### R2 — Exact ACTIVE Human paid grant

Load exact ACTIVE grant from `AtomicPaidApiGrantStore` and require:

```text
actor_kind = BRAIN
brain_id/provider/model exact
operation = PLAN for the M11.3C real proof
authorized artifact path/blob exact
workspace exact
max_calls = 1
unexpired
unconsumed
```

No grant creation or mutation occurs at this stage.

### R3 — Authorized artifact + deterministic request context

Resolve the grant-bound authorized artifact from the local canonical control tracking ref and verify exact Git blob identity.

Use the authorized artifact as mandatory TASK context. Do not discover arbitrary repository files.

The real proof instruction is fixed in code, bounded, proposal-only, and asks for a concise PLAN artifact demonstrating advisory reasoning only. It must not ask for patches, tool use, credentials, hidden reasoning, or execution.

### R4 — Exact local assets and counters

Load the proof-locked MiniMax asset bundle from the external runtime directory.

Extend `MiniMaxM3LocalProviderInputCounter` with the minimal `TokenCounter.count(text)` surface backed by the already-loaded pinned tokenizer so the same trusted object can be used by `ContextBuilder` as an exact context counter and by M11.2C.1 as the full provider-input counter.

No second tokenizer implementation, no download, no fallback counter.

Require:

```text
counter exact class
counter provider/model exact
counter proof_lock exact
context_build.token_count_is_exact = true
full ProviderInputCountEvidence.token_count_is_exact = true
```

### R5 — Fresh Brain capacity evidence

Load exactly two explicit `RuntimeCapacityRecord` values from the external runtime capacity store:

1. one Human-selected SUBSCRIPTION Brain (`--subscription-brain-id`), and
2. the grant-bound MiniMax PAID_API Brain (`grant.brain_id`).

Both must:

```text
actor_kind = BRAIN
be FRESH at execution time
match the caller-supplied exact record fingerprint
have no future timestamp
```

State requirements:

```text
subscription Brain = QUOTA_EXHAUSTED or UNAVAILABLE
paid MiniMax Brain = AVAILABLE
```

UNKNOWN, LIMITED, expired, missing, malformed, wrong actor kind, wrong fingerprint, or wrong state => fail before grant consumption/provider construction.

### R6 — Deterministic M10 dispatch proof

Build exactly two `BrainDispatchCandidate` values with the granted PLAN operation.

Construct base request with:

```text
allow_paid_api = false
```

Prove the base dispatch does NOT select the paid candidate and that no runnable subscription candidate wins.

Only the existing `execute_paid_api_brain_escape` may construct the effective `allow_paid_api=true` request after exact grant validation.

M10 must then select the grant-bound PAID_API Brain.

### R7 — Hard token/call budget gate

Before provider invocation require:

```text
max_calls = 1
exact context count within ModelRequest max_input_tokens
exact full provider input count within ModelRequest max_input_tokens
ModelRequest max_input_tokens <= grant.max_input_tokens
ModelRequest max_output_tokens <= grant.max_output_tokens
```

No currency estimate is a security boundary.

### R8 — Consume-before-call + exactly one provider invocation

Reuse `execute_paid_api_brain_escape` unchanged for the terminal transition:

```text
ACTIVE
  -> durable CONSUMED
  -> exactly one ModelGateway.invoke
```

No retry. No provider failover. No restoration to ACTIVE on timeout/error/crash.

A crash after consume and before/during provider response loses the authorization and requires a new Human grant.

### R9 — Correlated operational proof + bounded proposal artifact

On successful response, call `build_paid_api_operational_proof` using the ORIGINAL pre-call `ProviderInputCountEvidence`; never perform a second provider-input count.

Require exact correlation:

```text
local pre-call input tokens
== ModelResponse.input_tokens
== UsageRecord.provider_input_tokens

ModelResponse.output_tokens
== UsageRecord.provider_output_tokens

ledger persisted = true
provider request id correlated
grant terminal state = CONSUMED
response status = SUCCESS
```

Persist two external-runtime artifacts atomically/durably under the exact task/grant namespace:

```text
paid_api_proofs/TASK-N/<sha256(grant_id)>/proposal.md
paid_api_proofs/TASK-N/<sha256(grant_id)>/proof.json
```

`proposal.md` contains only the validated `ModelResponse.content` already stripped/rejected for reasoning markers by the provider contract.

`proof.json` contains bounded semantic evidence and hashes/logical paths only. It must not contain API keys, auth headers, raw request payloads, raw provider bodies, hidden reasoning, absolute machine paths, or cookies.

The proof receipt must bind:

```text
runtime main SHA
control commit SHA
proof lock path/blob/fingerprint
subscription capacity record fingerprint
paid capacity record fingerprint
preflight fingerprint
operational proof fingerprint
logical proposal path + proposal SHA-256
logical proof path
grant consumed = true
provider call count = 1
retry count = 0
executor authority created = false
```

### R10 — Replay rejection

A second invocation using the same consumed grant MUST fail before provider construction/invocation.

The implementation tests must prove with a spy/fake provider call counter that replay reaches zero additional calls.

The real post-merge proof procedure will deliberately run the exact same command a second time after the successful call and capture the bounded `GRANT_ALREADY_CONSUMED / NO_PROVIDER_CALL` rejection evidence.

## 7. Failure Semantics

Before R8 consumption: any validation failure => no spend, grant remains ACTIVE.

At/after successful consume:

```text
provider timeout/error          -> grant remains CONSUMED
ledger failure                  -> grant remains CONSUMED
proof artifact write failure    -> grant remains CONSUMED
process crash                   -> grant remains CONSUMED
```

Never retry automatically. Never restore grant to ACTIVE. Never fail over to another paid provider.

## 8. Secret and Output Safety

Real credential value may exist only in provider process memory/environment after R0-R7 pass.

Forbidden from stdout/stderr, Git, RESULT, proof receipts, proposal metadata, logs, fingerprints, or exception strings:

```text
MINIMAX_API_KEY value
Authorization/Bearer value
cookies
raw request headers
raw provider response body
hidden reasoning/reasoning_content
absolute runtime paths
```

Only `CREDENTIAL_SOURCE: env:MINIMAX_API_KEY` and a boolean presence indicator may be exposed before the call.

## 9. Test Strategy — Mandatory No-Spend

TASK-062 executor tests MUST NOT use internet/network and MUST NOT read a real `MINIMAX_API_KEY`.

Tests use fake/local provider/transport and temporary runtime state to prove:

```text
exact R0-R10 ordering
no provider construction before all pre-call gates
fresh Brain capacity proof
subscription unavailable -> paid selected only after grant
context exact counter using pinned tokenizer seam
full provider-input count budget
consume-before-call
exactly one gateway invocation
no retry/failover
successful operational receipt correlation
proposal/proof external persistence without secret/path leakage
provider error still leaves CONSUMED
ledger failure still leaves CONSUMED
replay rejected with zero extra provider calls
paid Executor impossible
```

Static/regression tests must make actual network transport fatal if reached.

Full repository suite required.

## 10. Writable Scope

Implementation must remain within:

```text
bridge.py
src/aios_bridge/minimax_m3_input_counter.py
src/aios_bridge/paid_api_real_escape.py
tests/aios_bridge/test_minimax_m3_input_counter.py
tests/aios_bridge/test_paid_api_real_escape.py
tests/test_bridge_paid_api_real_escape.py
```

Bridge publication may additionally create `.ai/results/RESULT-062.md` only.

No edits to `paid_api_brain_escape.py`, `paid_api_operational_proof.py`, grant semantics, ModelGateway, MiniMax provider, dispatch engine, executor transports, H-Series, or TASK-061 artifacts.

## 11. Acceptance Boundary

TASK-062 PASS means:

```text
HARNESS_IMPLEMENTED: YES
HARNESS_TESTED_WITH_FAKE_PROVIDER: YES
FULL_REPO_GREEN: YES
REAL_PAID_API_CALL_DURING_TASK: NO
REAL_GRANT_CONSUMED_DURING_TASK: NO
M11.3C_LIVE_PROOF_COMPLETE: NO
```

M11.3C becomes fully complete only after:

1. TASK-062 PASS + Human merge,
2. Human provisions/verifies local pinned assets and credential,
3. Human records fresh exact Brain capacity evidence,
4. Human explicitly creates one bounded paid grant,
5. Human explicitly authorizes the live `paid-proof-execute` command,
6. exactly one real MiniMax call succeeds and proof artifacts correlate,
7. the same-grant replay is rejected before provider call,
8. ChatGPT audits the runtime proof receipt.

No executor is authorized to perform steps 2-7 merely because TASK-062 exists or is RUN/FIX-authorized.
