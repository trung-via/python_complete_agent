# TASK-057 — M11.2C.2 Pinned Local MiniMax-M3 Asset Renderer + Exact Input Counter Reissue Blueprint

STATUS: LOCKED_FOR_IMPLEMENTATION
MILESTONE: M11.2C.2 — PINNED LOCAL MINIMAX-M3 PROVIDER-INPUT COUNTER
BASELINE_MAIN_SHA: 867cb5cdb730639db93a1f184f065dbb97230cd0
TARGET_BRANCH: ai/task-057

## Recovery Status

TASK-056 attempted execution but E4 observed zero worktree delta and failed closed at the post-executor Git/scope gate.

```text
TASK_056_RETRY: FORBIDDEN
TASK_056_REACTIVATION: FORBIDDEN
TASK_056_PUBLICATION_AUTHORITY: NONE
TASK_057: SOLE NEW IMPLEMENTATION AUTHORITY FOR M11.2C.2
```

The exact child reason is not observable from retained Bridge evidence. Do not claim one.

Independent audit identified a structural bounded-context contradiction in TASK-056: the child was forbidden network/download access yet was required to implement exact whitespace/newline behavior from an upstream 237-line Jinja template whose exact bytes were not present in the bounded repository context. TASK-057 removes that contradiction rather than asking the child to reconstruct upstream text from prose.

Historical anchors only:

```text
TASK_056_PATH: .ai/tasks/TASK-056.md
TASK_056_BLOB_SHA: c14991ade6d981b06aae55908ae9951bf1d14f2b
TASK_056_BLUEPRINT_PATH: .ai/context/TASK-056-M11.2C2-PINNED-LOCAL-MINIMAX-M3-INPUT-COUNTER-BLUEPRINT.md
TASK_056_BLUEPRINT_BLOB_SHA: 4888eafc926463761a0d4680f69a5b8156c9cd8b
```

## Core Decision

Do NOT hard-code or manually specialize the official MiniMax-M3 Jinja template in Python.

The external runtime asset bundle must contain BOTH:

```text
chat_template.jinja
tokenizer.json
```

The counter:

```text
ModelRequest
  -> existing AIOS render_messages(ModelRequest)
  -> validate exact [system,user] text-only shape
  -> render the PINNED LOCAL chat_template.jinja itself
     with add_generation_prompt=True
     and no tools / no explicit thinking_mode
  -> encode rendered prompt with PINNED LOCAL tokenizer.json
     add_special_tokens=False
  -> exact ProviderInputCountEvidence
```

This makes the upstream template bytes runtime data protected by the asset manifest, not knowledge the executor must reproduce.

## Official Source Pin

```text
OFFICIAL_MODEL_REPOSITORY: MiniMaxAI/MiniMax-M3
OFFICIAL_MODEL_REVISION: 3a41b311ffa5719cef48fed3974ccf2cc03733ea
OFFICIAL_CHAT_TEMPLATE_PATH: chat_template.jinja
OFFICIAL_TOKENIZER_PATH: tokenizer.json
PROVIDER_ID: minimax
MODEL_ID: MiniMax-M3
```

Mutable upstream `main` is forbidden as runtime authority.

At the pinned revision, the official template behavior relevant to the exact AIOS two-message shape is externally verified:
- an initial `system` message is assigned to the lower-priority developer slot;
- without a `root` message, the template emits its default MiniMax system identity and thinking instructions;
- the remaining user message is emitted in the user slot;
- `add_generation_prompt=True` emits the AI generation prefix;
- when `thinking_mode` is not supplied, adaptive/default semantics apply.

TASK-057 implementation must not duplicate those upstream strings in source code. It must load/render the pinned local template asset.

## Runtime Asset Layout

Expected out-of-band provisioned directory:

```text
<asset_root>/minimax-m3/3a41b311ffa5719cef48fed3974ccf2cc03733ea/
    asset-manifest.json
    chat_template.jinja
    tokenizer.json
```

Exact manifest semantic fields:

```json
{
  "schema_version": "1",
  "source_repository": "MiniMaxAI/MiniMax-M3",
  "source_revision": "3a41b311ffa5719cef48fed3974ccf2cc03733ea",
  "chat_template_path": "chat_template.jinja",
  "chat_template_sha256": "<lowercase sha256>",
  "tokenizer_path": "tokenizer.json",
  "tokenizer_sha256": "<lowercase sha256>"
}
```

Manifest validation:
- exact field set, no missing/extra keys;
- exact source repository/revision/path constants;
- both digests exact lowercase 64-hex;
- manifest, template, tokenizer each regular file and non-symlink;
- template and tokenizer paths must resolve directly under supplied asset directory;
- template size ceiling before decode/parse, e.g. 1 MiB;
- tokenizer size ceiling before parse, e.g. 32 MiB;
- SHA-256 actual bytes must equal manifest digest for BOTH files;
- template must decode strict UTF-8;
- no auto-repair and no bundle mutation.

The manifest is an operator-provisioned runtime receipt. Runtime re-hash protects against drift between the receipt and actual local bytes. M11.3 later supplies the operational cross-check against provider-reported input tokens.

## Dependencies

Pin in `requirements.txt`:

```text
tokenizers==0.23.1
Jinja2==3.1.6
```

Both are runtime-local engines only.

Production module must lazy-import them so repository tests can run without network/download or dependency installation during E4. Missing dependency at real counter construction must fail closed with a bounded error.

Forbidden:
- transformers;
- huggingface_hub;
- trust_remote_code;
- model weights;
- provider token-count endpoint;
- runtime dependency/network installation.

## Exact Message Boundary

Counter MUST call existing:

```python
external_brain.prompt.render_messages(model_request)
```

Then require exact shape:

```text
len(messages) == 2
messages[0] exact dict keys == {role, content}
messages[0].role == system
messages[0].content exact str
messages[1] exact dict keys == {role, content}
messages[1].role == user
messages[1].content exact str
```

No assistant history, third message, tools, images, multipart content, root/developer caller role, or tool calls.

Any shape drift fails closed BEFORE template rendering/tokenization.

## Sandboxed Local Template Rendering

Load the already-validated local UTF-8 `chat_template.jinja` bytes and render using a sandboxed Jinja environment with strict undefined semantics.

Runtime context must be bounded to the official chat-template inputs needed here:

```text
messages = exact two validated dictionaries
tools = None
add_generation_prompt = True
```

Do NOT explicitly pass `thinking_mode`, so pinned-template default/adaptive behavior is preserved.

Provide a fail-closed `raise_exception` helper because the official template references it on invalid branches. No filesystem/network/provider/global application objects may be exposed to the template.

Use sandboxing and no filesystem template loader. Compile/render only the already-read local template string.

Template rendering must be deterministic for identical validated asset bytes + ModelRequest.

## Exact Local Tokenization

Load only the already-validated local `tokenizer.json` using the `tokenizers` library.

After rendering:

```text
encoding = tokenizer.encode(rendered_prompt, add_special_tokens=False)
counted_input_tokens = len(encoding.ids)
```

The template itself emits its framing/special-token strings. No second automatic special-token layer is permitted.

## Production Counter

Create exact concrete type:

```text
MiniMaxM3LocalProviderInputCounter
```

Required properties:

```text
provider_id == minimax
model_id == MiniMax-M3
is_exact is True
counter_id deterministically binds:
    official source revision
    chat_template_sha256
    tokenizer_sha256
```

`count_request(ModelRequest)` returns TASK-055 `ProviderInputCountEvidence` with:
- exact provider/model;
- canonical ModelRequest fingerprint;
- local rendered+encoded input count;
- exact counter_id;
- token_count_is_exact=True.

No rendered prompt/template/tokenizer bytes are persisted in evidence, logs, RESULT, or receipts.

## Trusted-Local Registration

TASK-055 production registry is empty.

TASK-057 may register exactly:

```text
MiniMaxM3LocalProviderInputCounter
```

Exact-type semantics remain:

```text
EXACT REGISTERED TYPE: ACCEPTABLE
SUBCLASS: REJECT
WRAPPER: REJECT
PROTOCOL-ONLY OBJECT: REJECT
CALLER-INJECTED TOKENIZER/JINJA BACKEND: FORBIDDEN
```

The production class owns asset validation, sandbox creation, template compilation, and tokenizer loading internally.

Private module-local loader functions may be monkeypatched in tests only. There must be no public backend/callable constructor injection seam.

## Runtime Network/Credential Prohibition

Production counter module must not import/use:

```text
requests
urllib/http clients
httpx
aiohttp
socket
huggingface_hub
provider adapters/transports
subprocess for fetch/install
API keys
Authorization headers
cookies
```

Construction + counting are local file/CPU work only.

## Testability Without Runtime Assets

TASK-057 execution itself must not download official assets.

Tests use temporary synthetic local asset bundles and private-loader monkeypatches where needed. They must verify the generic asset-hash/template-render/tokenizer-count pipeline, not copy the copyrighted upstream template into repository fixtures.

Required tests include at minimum:

```text
EXACT_PROVIDER_MODEL_IDS
IS_EXACT_TRUE
COUNTER_ID_BINDS_REVISION_TEMPLATE_SHA_TOKENIZER_SHA
MANIFEST_EXACT_FIELDS
MANIFEST_SOURCE_REVISION_PATHS_EXACT
MANIFEST_SYMLINK_REJECTED
TEMPLATE_SYMLINK_REJECTED
TOKENIZER_SYMLINK_REJECTED
TEMPLATE_MISSING_REJECTED
TOKENIZER_MISSING_REJECTED
TEMPLATE_OVERSIZE_REJECTED_BEFORE_RENDER
TOKENIZER_OVERSIZE_REJECTED_BEFORE_PARSE
TEMPLATE_SHA_MISMATCH_REJECTED
TOKENIZER_SHA_MISMATCH_REJECTED
TEMPLATE_INVALID_UTF8_REJECTED
MISSING_JINJA2_FAILS_CLOSED
MISSING_TOKENIZERS_FAILS_CLOSED
RENDER_MESSAGES_CALLED
EXACT_TWO_MESSAGE_SHAPE_REQUIRED
EXTRA_MESSAGE_REJECTED_BEFORE_TEMPLATE
WRONG_ROLE_REJECTED_BEFORE_TEMPLATE
NON_STRING_CONTENT_REJECTED_BEFORE_TEMPLATE
JINJA_SANDBOX_USED
STRICT_UNDEFINED_USED
TOOLS_NONE
ADD_GENERATION_PROMPT_TRUE
THINKING_MODE_NOT_EXPLICITLY_PASSED
TOKENIZER_ADD_SPECIAL_TOKENS_FALSE
COUNT_EQUALS_ENCODING_IDS_LENGTH
EVIDENCE_FINGERPRINT_EXACT
EVIDENCE_CONTAINS_NO_PROMPT_OR_ASSET_BYTES
PRODUCTION_REGISTRY_ACCEPTS_EXACT_CLASS
SUBCLASS_REJECTED
NO_NETWORK_IMPORT_SURFACE
NO_CREDENTIAL_READ_SURFACE
NO_REAL_PROVIDER_CALL
FULL_REPO_TESTS_PASS
```

Tests may use a minimal synthetic Jinja template that reveals supplied variables and a fake tokenizer object returned from a PRIVATE loader function. This is test-only and must not create public production injection authority.

## M11.3 Operational Correlation

TASK-057 does NOT call MiniMax.

M11.3 later must provision the real pinned assets out-of-band, construct the exact trusted counter, and on the ONE authorized paid provider call compare:

```text
LOCAL_PRECALL_COUNT == PROVIDER_REPORTED_INPUT_TOKENS
```

Mismatch => M11.3 FAIL, grant stays CONSUMED, no retry.

## Exact Writable Scope

Executor may create/modify exactly:

```text
src/aios_bridge/minimax_m3_input_counter.py
src/aios_bridge/provider_input_budget.py
requirements.txt
tests/aios_bridge/test_minimax_m3_input_counter.py
```

Bridge may separately publish `.ai/results/RESULT-057.md`.

## Bounded Read-Only Scope

```text
src/aios_bridge/provider_input_budget.py
src/aios_bridge/paid_api_brain_escape.py
src/aios_bridge/external_brain/contracts.py
src/aios_bridge/external_brain/prompt.py
src/aios_bridge/external_brain/providers/minimax.py
requirements.txt
.ai/decisions/ADR-036-M11-EXTERNAL-API-ESCAPE-HATCH-ARCHITECTURE-LOCK.md
.ai/reviews/REVIEW-055.md
```

No external source lookup is required for implementation. Exact upstream template bytes are intentionally runtime asset data, not executor context.

## Child Executor Role Lock

The Bridge E4-spawned Codex process is the bounded implementation Executor child, NOT the visible `aios-worker` operator UI.

```text
visible Codex + aios-worker skill = operator UI
Bridge E4 spawned Codex process  = bounded implementation Executor
```

The child MUST implement now, modify only allowed paths, run targeted tests, leave authorized dirty delta, and exit normally.

It MUST NOT invoke the worker adapter again, commit, push, publish, merge, download upstream assets, install packages from network, access credentials, or make provider/network calls.

## Explicit Out of Scope

```text
TASK_056_RETRY: FORBIDDEN
REAL_PAID_API_CALL: NO
REAL_MINIMAX_CALL: NO
TOKEN_COUNT_ENDPOINT: NO
ASSET_DOWNLOAD_DURING_TASK: NO
ASSET_PROVISIONING_COMMAND: NO
M11.3: NOT_STARTED
PROVIDER_CHANGE: NO
GATEWAY_CHANGE: NO
PROMPT_RENDERER_CHANGE: NO
GRANT_CHANGE: NO
GRANT_STORE_CHANGE: NO
M10_CHANGE: NO
H_SERIES: DEFERRED
```

## Completion Boundary

After publication:

```text
STOP
NEXT: Review TASK-057
```
