# TASK-052 — M11.2B Human Paid API Grant Command + Exact Runtime Binding — Implementation Blueprint

STATUS: LOCKED BLUEPRINT
MILESTONE: M11.2B — HUMAN BRIDGE PAID API GRANT COMMAND
BASELINE_MAIN_SHA: 15a26f7a2810a5540bed0a3f7ad8f662b04533d4
TARGET_BRANCH: ai/task-052
EXECUTOR_MODE: UNIFIED_AIOS_WORKER_DUAL_EXECUTOR

## Contract Anchors

```text
ADR_036_PATH: .ai/decisions/ADR-036-M11-EXTERNAL-API-ESCAPE-HATCH-ARCHITECTURE-LOCK.md
ADR_036_BLOB_SHA: cf71c571d8e3fd611ea07d21f15ad0bf90ef6ecc
ADR_038_PATH: .ai/decisions/ADR-038-DEFAULT-DUAL-EXECUTOR-TASK-AUTHORING-POLICY-LOCK.md
ADR_038_BLOB_SHA: 72d38bf2f2ff5a07e7b63322116ad87622349df1
M11_1_PATH: src/aios_bridge/paid_api_grant.py
M11_1_BLOB_SHA: 7f1e1fe666154a9b17013a2cb084db9ce36f134f
M11_2A_PATH: src/aios_bridge/runtime_paid_api_grant.py
M11_2A_BLOB_SHA: a3c7a446ff0f8195e68640493900776334a9e551
REVIEW_051_PATH: .ai/reviews/REVIEW-051.md
REVIEW_051_BLOB_SHA: a441e954f9fa84b33d5f0d763f498702e899ae70
```

## Objective

Expose the first explicit Human Bridge control surface that creates one bounded paid-API Brain grant and persists it through the merged `AtomicPaidApiGrantStore`.

TASK-052 is authorization UX + exact runtime binding only.

It MUST NOT:
- set `BrainDispatchRequest.allow_paid_api = true`;
- dispatch a Brain;
- select a paid candidate;
- invoke `ModelGateway` or any provider;
- read provider credentials;
- perform a real paid API call;
- create Executor authority;
- modify M10 dispatch semantics;
- implement M11.2C or M11.3.

## Authority Model

The Bridge command invocation itself is the Human spend-authorization boundary.

A task marker, dispatch recommendation, exhausted subscription capacity, or presence of provider credentials MUST NOT create a grant.

The command MUST require an explicit confirmation flag:

```text
--confirm-paid-api-spend
```

Without this flag, the command fails before any grant state is created.

The command creates exactly one fresh one-shot grant per successful invocation. No automatic retry on collision or activation failure.

## Runtime Path

Extend `get_runtime_paths()` with exactly:

```python
"paid_api_grants": rdir / "paid_api_grants"
```

Extend `ensure_dirs()` so this external runtime directory exists.

Add:

```python
def get_paid_api_grant_store(repo_root: Path | None = None) -> AtomicPaidApiGrantStore:
    paths = get_runtime_paths(repo_root)
    return AtomicPaidApiGrantStore(
        grant_root=paths["paid_api_grants"],
        workspace_id=get_workspace_id(repo_root),
    )
```

No grant JSON may be written under the repository worktree or `.ai/`.

## Required Imports

`bridge.py` may import exactly the merged contracts needed for this command:

```python
from src.aios_bridge.continuity.state import BrainOperation
from src.aios_bridge.paid_api_grant import PaidApiGrant
from src.aios_bridge.runtime_paid_api_grant import AtomicPaidApiGrantStore
```

Existing `ArtifactRef` / `ContinuityStateValidationError` imports may be reorganized without semantic change.

## Human Create Command

Add CLI command:

```text
paid-grant-create
```

Canonical usage:

```powershell
.\venv\Scripts\python.exe .\bridge.py paid-grant-create 52 `
  --brain-id minimax-brain `
  --provider-id minimax `
  --model-id MiniMax-M3 `
  --operation PLAN `
  --artifact-path .ai/tasks/TASK-052.md `
  --max-input-tokens 12000 `
  --max-output-tokens 4000 `
  --ttl-seconds 600 `
  --confirm-paid-api-spend
```

All fields below are required. There are no implicit defaults for provider/model/operation/artifact/budgets/TTL.

### Exact CLI arguments

```text
task_id                  positional int
--brain-id               required str
--provider-id            required str
--model-id               required str
--operation              required, choices = exact continuity BrainOperation values
--artifact-path          required str
--max-input-tokens       required int
--max-output-tokens      required int
--ttl-seconds            required int
--confirm-paid-api-spend required boolean flag
```

Forbidden CLI authority inputs:

```text
--grant-id               FORBIDDEN
--artifact-blob-sha      FORBIDDEN
--workspace-id           FORBIDDEN
--actor-kind             FORBIDDEN
--max-calls              FORBIDDEN
--api-key                FORBIDDEN
--authorization-header   FORBIDDEN
--token                   FORBIDDEN
--cookie                  FORBIDDEN
```

Reason:
- `grant_id` must be fresh and Bridge-generated;
- artifact blob must be resolved from canonical `ai-control` by Bridge;
- workspace must be the current deterministic Bridge workspace;
- actor kind is fixed BRAIN;
- max calls is fixed 1 by M11.1;
- credentials are outside the grant/control plane.

## Grant ID

Bridge generates exactly one fresh grant ID per invocation using `secrets.token_hex()`.

Canonical shape:

```text
grant-task-052-<lowercase random hex>
```

It must satisfy the existing M11.1 `grant_id` contract and stay below its maximum length.

Do not retry grant-ID generation automatically if store activation collides or fails. Fail closed and require a new explicit Human command.

## TTL Contract

TASK-052 defines an operational Human-grant TTL bound without changing M11.1 serialization semantics.

```text
MIN_PAID_API_GRANT_TTL_SECONDS = 1
MAX_PAID_API_GRANT_TTL_SECONDS = 900
```

`--ttl-seconds` must be exact CLI integer within `[1, 900]`.

Bridge obtains exactly one wall-clock observation:

```python
now_epoch_seconds = int(time.time())
expires_at_epoch_seconds = now_epoch_seconds + ttl_seconds
```

The persisted grant stores only the absolute expiry already defined by M11.1.

The runtime store still never reads wall clock internally.

No default TTL and no grace period.

## Exact Artifact Binding

`paid-grant-create` MUST:

1. load Bridge config;
2. fetch canonical control branch;
3. resolve `--artifact-path` only from configured `ai-control` remote-tracking ref using existing Bridge Git helpers;
4. require an exact existing Git blob SHA;
5. never accept a Human-supplied blob SHA;
6. construct the `PaidApiGrant` using that exact path/blob pair;
7. bind exact current `workspace_id`;
8. bind exact CLI task/brain/provider/model/operation/budgets;
9. set `actor_kind = DispatchActorKind.BRAIN`;
10. set `max_calls = 1`.

The `PaidApiGrant` constructor remains the canonical semantic validator for artifact path, identifiers, token bounds, operation, workspace, and fingerprint.

If the artifact is absent or invalid, fail before store activation.

The command MUST NOT infer authorization from TASK content such as `allow_paid_api:true`.

## Exact Create Flow

```text
Human command + --confirm-paid-api-spend
↓
validate TTL and explicit CLI values
↓
fetch ai-control
↓
resolve exact artifact path -> exact blob SHA
↓
compute current workspace_id
↓
observe now_epoch_seconds once
↓
generate one fresh grant_id
↓
construct immutable PaidApiGrant
↓
AtomicPaidApiGrantStore.activate(grant, now_epoch_seconds=now)
↓
AtomicPaidApiGrantStore.require_active(grant, now_epoch_seconds=now)
↓
print safe receipt metadata
STOP
```

No dispatch/provider call follows creation.

## Safe Command Output

On success print a bounded receipt containing only non-secret grant metadata:

```text
[PAID API GRANT ACTIVE]
TASK_ID
GRANT_ID
ACTOR_KIND: BRAIN
BRAIN_ID
PROVIDER_ID
MODEL_ID
BRAIN_OPERATION
AUTHORIZED_ARTIFACT_PATH
AUTHORIZED_ARTIFACT_BLOB_SHA
MAX_INPUT_TOKENS
MAX_OUTPUT_TOKENS
MAX_CALLS: 1
EXPIRES_AT_EPOCH_SECONDS
WORKSPACE_ID
GRANT_FINGERPRINT
HUMAN_SPEND_AUTHORIZATION: YES
PAID_API_DISPATCH_ENABLED: NO
PROVIDER_CALL_STARTED: NO
```

Never print raw grant JSON, environment values, API keys, headers, cookies, or provider request/response bodies.

## Read-Only Status Command

Add CLI command:

```text
paid-grant-status <task_id> --grant-id <grant_id>
```

This command is strictly read-only and non-authorizing.

It uses the exact runtime store and reports one of:

```text
RUNTIME_STATE: ACTIVE
RUNTIME_STATE: CONSUMED
RUNTIME_STATE: NONE
```

For ACTIVE, it may additionally report:

```text
USABILITY: UNEXPIRED | EXPIRED
```

using a read-only `int(time.time())` comparison against the stored expiry.

It MUST NOT mutate, consume, refresh, extend, recreate, dispatch, or call a provider.

If runtime state is malformed or dual ACTIVE/CONSUMED corruption exists, fail closed through the store.

## No Revoke/Delete in M11.2B

Do NOT add:
- `paid-grant-revoke`;
- grant deletion;
- ACTIVE cleanup on expiry;
- REVOKED state;
- TTL extension/refresh.

Reason: M11.2A locked only ACTIVE/CONSUMED. Inventing deletion/revocation here would change the state machine and weaken audit/replay semantics.

Short required TTL bounds stale Human authorizations. A future explicit contract may add revocation only if operational evidence requires it.

## Security / Side-Effect Boundaries

TASK-052 MUST preserve:

```text
PAID_API_DEFAULT: DENY
AUTO_ENABLE_FROM_TASK: FORBIDDEN
AUTO_ENABLE_FROM_DISPATCH: FORBIDDEN
AUTO_ENABLE_FROM_CAPACITY_EXHAUSTION: FORBIDDEN
AUTO_RETRY: FORBIDDEN
AUTO_FAILOVER_TO_SECOND_PAID_PROVIDER: FORBIDDEN
AUTO_EXECUTOR_API: FORBIDDEN
```

`paid-grant-create` may mutate only the external paid grant runtime directory.

It MUST NOT mutate:
- Executor authorization;
- Executor leases;
- capacity observations;
- hot-handoff state;
- task/review/result artifacts;
- task branch;
- `main`;
- provider usage ledger;
- dispatch request/result.

## Exact Writable Files

Executor may modify/create exactly:

```text
bridge.py
tests/test_bridge_paid_api_grant.py
```

Bridge publication may generate `.ai/results/RESULT-052.md` only.

## Bounded Read Scope for Executor

In addition to Bridge-delivered TASK/context artifacts, Executor may read exactly:

```text
bridge.py
src/aios_bridge/paid_api_grant.py
src/aios_bridge/runtime_paid_api_grant.py
src/aios_bridge/continuity/state.py
tests/test_bridge_dispatch.py
```

Do not broaden repository inspection beyond what is necessary for TASK-052.

## Required Tests

Mechanically prove at minimum:

1. runtime paths include external `paid_api_grants` and `ensure_dirs()` creates it;
2. `get_paid_api_grant_store()` binds exact current workspace;
3. create command without `--confirm-paid-api-spend` fails before activation;
4. create command requires every provider/model/operation/artifact/budget/TTL argument;
5. parser does not expose credential, blob, workspace, actor-kind, max-calls, or user grant-ID authority flags;
6. TTL lower/equality/upper boundaries: 1 and 900 accepted, 0 and 901 rejected;
7. Bridge observes wall clock once for create and persists exact absolute expiry;
8. grant ID is Bridge-generated, fresh-format, and no automatic collision retry occurs;
9. actor kind is exactly BRAIN and max_calls exactly 1;
10. exact task/workspace/brain/provider/model/operation/token budget binding;
11. artifact blob is resolved from canonical control branch and Human cannot provide/override blob SHA;
12. missing artifact fails before activation;
13. invalid artifact path/contract fails before persisted ACTIVE state;
14. successful create activates then exact-requires the same grant;
15. success output includes safe receipt metadata and explicitly says dispatch/provider call remain disabled/not started;
16. provider credential environment values are never copied into persisted grant or output;
17. create command does not invoke dispatch, ModelGateway, provider, capacity mutation, Executor auth/lease mutation, or publish;
18. activation failure is surfaced with exactly one store activation attempt and no retry;
19. `paid-grant-status` reports ACTIVE/EXPIRED/CONSUMED/NONE read-only;
20. status never activates/consumes/refreshes/deletes state;
21. malformed/corrupt runtime state fails closed;
22. no real paid API call;
23. no M11.2C wiring and no `allow_paid_api=true` mutation;
24. full repository suite remains green.

## Targeted Test

```powershell
.\venv\Scripts\python.exe -m pytest tests/test_bridge_paid_api_grant.py -q
```

Executor runs only this targeted suite. Bridge publication owns the full repository suite.

## Acceptance

```text
HUMAN_CONFIRMATION_GATE: PASS
BRIDGE_GENERATED_GRANT_ID: PASS
CANONICAL_CONTROL_BLOB_BINDING: PASS
CURRENT_WORKSPACE_BINDING: PASS
BRAIN_ONLY_GRANT: PASS
MAX_CALLS_ONE: PASS
EXPLICIT_TOKEN_BOUNDS: PASS
SHORT_EXPLICIT_TTL: PASS
EXTERNAL_RUNTIME_PERSISTENCE: PASS
STRICT_READBACK: PASS
READ_ONLY_STATUS: PASS
NO_CREDENTIAL_PERSISTENCE: PASS
NO_DISPATCH_ENABLEMENT: PASS
NO_PROVIDER_CALL: PASS
NO_EXECUTOR_AUTHORITY: PASS
NO_RETRY_OR_REROUTE: PASS
M11_2C_NOT_IMPLEMENTED: PASS
M11_3_NOT_IMPLEMENTED: PASS
TARGETED_TESTS: PASS
FULL_REPO_TESTS: PASS
REGRESSIONS: 0
FINAL_INDEPENDENT_AUDIT: PASS
```

## Completion Boundary

After successful Bridge publication:

```text
STOP
NEXT: Review TASK-052 in ChatGPT
```

Do not begin M11.2C automatically.
