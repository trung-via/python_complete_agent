task_id: TASK-247
revision: 1

goal: >
  Canonicalize exactly one fresh Human-operated generation-7 authorization for the already-published
  schema-version-4 TikTok PDP bounded title-local commerce-root diagnostic. Bind the one-shot attempt
  only to the fixed selected listing and exact published TASK-246 source, preserve the V4 root
  selection/quorum contract without production-code changes, preserve TASK-137 browser lifecycle
  authority and all Product Intelligence evidence/truth/acquisition/action boundaries, and prevent
  historical TASK-246 governance assertions from freezing the new current handoff. TASK-247 performs
  no live TikTok/CDP execution.

problem: >
  TASK-246 / RUN-246-002 / REVIEW-246-001 are reviewed, published, and canonical main equals exact
  reviewed candidate 60da55d5241c7b4c433d9b2d7d5d3725556f443c. TASK-246 reconciled the sole
  Human-operated generation-6 attempt as consumed FAIL_CLOSED / NO_BOUNDED_PDP_ROOT with immutable
  schema-V3 artifact provenance and then hardened only diagnostic root selection to schema version 4.
  Published V4 preserves legacy root precedence EXPLICIT_PDP_ROOT -> MAIN ->
  MULTI_ANCHOR_COMMON_ANCESTOR and adds TITLE_LOCAL_COMMERCE_QUORUM as a fourth bounded fallback,
  with at most six light-DOM ancestors, at most 300 descendant observations per ancestor, fail-closed
  truncation semantics, and a strong quorum requiring currency-like signal plus BUY_LIKE/CART_LIKE
  structurally paired to a visible native/role button. Generation 7 is currently unauthorized;
  live_dom_diagnostic_authority is NONE; active_track.next_milestone is null; pending commitments are
  empty; and canonical handoff is
  HUMAN_BRAIN_FRESH_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_AUTHORIZATION.
  The smallest justified next work is authorization-only: one fresh Human-operated generation-7
  invocation using exact TASK-246 publication bytes, with no production or selector/quorum changes.
  Existing TASK-246 current-state governance regression must be converted to historical form so it
  cannot freeze the new active track/global handoff.

assumptions:
  - >
    Canonical main at authoring is 60da55d5241c7b4c433d9b2d7d5d3725556f443c.
  - >
    TASK-246 / RUN-246-002 / REVIEW-246-001 are Runtime PASS, semantic PASS, and exact reviewed
    candidate 60da55d5241c7b4c433d9b2d7d5d3725556f443c is published canonical main.
  - >
    Failed TASK-246 primary candidate 538fc65dd737e5c70e7b7ec93dfb6ab0bee8ba6f and RUN-246-001
    remain historical failed-run lineage only and are not execution authority.
  - >
    Generation 6 remains consumed history: one Human-operated attempt, remaining 0,
    FAIL_CLOSED / NO_BOUNDED_PDP_ROOT, immutable V3 artifact SHA256
    A5CD014DD2A9B10DD969843BA37369815A9595248DFB5DD29355376B51143764, size 27549,
    observed_at 2026-09-23T13:14:40.397608+00:00, evidence_authority NONE, with no invented process
    exit code.
  - >
    TASK-137 remains sole browser/CDP lifecycle and page-borrowing authority.
  - >
    TASK-231 remains sole public PDP acquisition contract; ProductCandidateSnapshot and
    SignalEvidence remain their existing semantic owners.
  - >
    CAPABILITY_IS_NOT_AUTHORITY, EVIDENCE_IS_NOT_PRODUCT_TRUTH,
    SOURCE_IDENTITY_IS_NOT_CANONICAL_IDENTITY, SNAPSHOT_IS_NOT_TREND and
    ONE_CAPABILITY_ONE_AUTHORITY remain mandatory.

scope:
  inspect:
    - docs/PYTHON_AGENT_MANIFESTO.md
    - docs/PYTHON_AGENT_CONSTITUTION.md
    - docs/PYTHON_AGENT_PRODUCT_CONTRACT.md
    - docs/CHATGPT_PROJECT_CONTRACT.md
    - docs/PHASE_8_PUBLIC_TIKTOK_PDP_ACQUISITION_CONTRACT.md
    - docs/PHASE_8_PUBLIC_TIKTOK_PDP_DOM_DIAGNOSTIC_GEN6_RESULT_RECONCILIATION_AND_V4_ROOT_HARDENING.md
    - docs/POST_M4_PRODUCT_INTELLIGENCE_ROADMAP.md
    - docs/POST_P5_P6_QUALITY_SCALE_ROADMAP.md
    - .ai/roadmap-state.yaml
    - .ai/tasks/TASK-137.yaml
    - .ai/tasks/TASK-245.yaml
    - .ai/tasks/TASK-246.yaml
    - src/product_intelligence/tiktok_pdp_dom_diagnostic.py
    - src/product_intelligence/cli.py
    - src/product_intelligence/adapters/tiktok_parsing.py
    - src/integrations/playwright/manager.py
    - src/integrations/playwright/session.py
    - tests/product_intelligence/test_tiktok_pdp_dom_diagnostic.py
    - tests/aios_bridge/test_aios_governance_state.py
  modify:
    - docs/PHASE_8_PUBLIC_TIKTOK_PDP_DOM_DIAGNOSTIC_GEN7_AUTHORIZATION.md
    - docs/POST_M4_PRODUCT_INTELLIGENCE_ROADMAP.md
    - docs/POST_P5_P6_QUALITY_SCALE_ROADMAP.md
    - .ai/roadmap-state.yaml
    - tests/aios_bridge/test_aios_governance_state.py

non_goals:
  - >
    Modify src/product_intelligence/tiktok_pdp_dom_diagnostic.py, src/product_intelligence/cli.py,
    src/product_intelligence/adapters/tiktok_parsing.py, src/integrations/playwright/manager.py,
    src/integrations/playwright/session.py, BrowserSession, PlaywrightBrowserManager,
    PlaywrightBrowserSession, TASK-137 lifecycle semantics, TikTokPdpCollector,
    TIKTOK_PDP_EXTRACTION_SCRIPT, ProductCandidateSnapshot, SignalEvidence, identity, catalog,
    persistence, scoring, ranking, recommendation, approval, market-test, action or outcome semantics.
  - >
    Execute TikTok/CDP, run generation 7, retry generation 6, navigate, refresh, click, type, scroll,
    search, switch variants, open another page, or perform any live marketplace operation during
    TASK-247 engineering execution.
  - >
    Change rootSelector, titleSelectors, priceSelectors, actionSelectors, root precedence,
    TITLE_LOCAL_COMMERCE_QUORUM semantics, BUY/CART pairing, six-ancestor/300-descendant bounds,
    truncation behavior, light-DOM-only policy, shadow/iframe traversal policy, artifact sanitation,
    failure taxonomy, or create-exclusive behavior.
  - >
    Hard-code or require generation-7 live outcome to use TITLE_LOCAL_COMMERCE_QUORUM, ancestor
    level 2, any particular root kind, observed class, data-testid, text hint or topology count.
  - >
    Grant arbitrary-target, replacement-target, inferred-identity, search, batch, variant-switching,
    acquisition, recurring diagnostic, automatic retry/resume, selector-repair, collector-repair,
    market-test, commerce-action, Product Truth or automatic successor authority.
  - >
    Treat V4 SUCCESS, selected root, title-local quorum, terminal output, artifact, structural
    signature or topology counts as canonical marketplace evidence, canonical price, canonical
    action capability, Product Truth, acquisition success, ranking evidence, approval evidence,
    collector readiness, trend evidence or commerce-action evidence.

constraints:
  hard:
    - >
      Classification is exactly
      FRESH_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_AUTHORIZATION_ONLY.
      TASK-247 is authorization/governance-only and must not modify production diagnostic/browser
      code.
    - >
      Record exact TASK-246 publication provenance using source_task_id TASK-246,
      source_run_id RUN-246-002, source_review_id REVIEW-246-001 and source_published_sha
      60da55d5241c7b4c433d9b2d7d5d3725556f443c.
    - >
      Set distinct diagnostic_v4_implementation_source_sha and generation_7_execution_source_sha
      both to exact reviewed/published TASK-246 source
      60da55d5241c7b4c433d9b2d7d5d3725556f443c. Do not use failed candidate
      538fc65dd737e5c70e7b7ec93dfb6ab0bee8ba6f as current implementation or execution authority.
    - >
      Preserve generations 1-6 as immutable consumed history. Generation 6 remains executed true,
      attempts 1, remaining 0, Human-owned, FAIL_CLOSED / NO_BOUNDED_PDP_ROOT, artifact_created
      true, evidence_authority NONE and live_dom_diagnostic_authority NONE; preserve exact V3
      artifact SHA256/size/observed_at and do not invent a generation-6 process exit code.
    - >
      Current generation-7 authorization state must not inherit generic generation-6 terminal
      fields as if generation 7 already executed. Only generation 7 receives current authorization
      fields: generation_7_authorized_diagnostic_attempts 1,
      generation_7_authorized_diagnostic_attempts_remaining 1,
      generation_7_diagnostic_execution_owner HUMAN_OPERATOR,
      generation_7_diagnostic_executed false, with no current diagnostic_outcome or
      diagnostic_failure_reason asserted before the attempt.
    - >
      Only exact reviewed TASK-247 publication may set diagnostic_authorization_generation 7,
      live_dom_diagnostic_authority ONE_SHOT_ATTACH_ONLY_EXACT_LISTING and generation_7_authorized
      true. Before exact publication, generation 7 remains unauthorized.
    - >
      Generation 7 is bound only to context p8-pilot-001-led-motion-tiktok-vn, source ID
      1731381331718341815 and exact selected listing
      https://shop.tiktok.com/vn/pdp/den-led-cam-bien-chuyen-dong-3-che-do-sang-sac-usb-c/1731381331718341815.
      No replacement product, arbitrary target, inferred identity, search, batch or variant-switch
      authority is granted.
    - >
      Bind generation 7 to the published V4 contract: schema_version 4, exact create-exclusive
      filename tiktok-pdp-dom-diagnostic-v4.json, carrier
      src/product_intelligence/tiktok_pdp_dom_diagnostic.py, canonical CLI subcommand
      tiktok-pdp-dom-diagnostic, evidence_authority NONE, one borrowed session, exactly one
      session.evaluate(DIAGNOSTIC_SCRIPT), zero navigation/refresh/click/type/scroll and TASK-137
      manager-mediated cleanup.
    - >
      Freeze V4 semantics exactly. Existing root-selection precedence remains
      EXPLICIT_PDP_ROOT -> MAIN -> MULTI_ANCHOR_COMMON_ANCESTOR ->
      TITLE_LOCAL_COMMERCE_QUORUM. The fallback still starts from the unique visible title anchor,
      checks ancestors in ascending narrowest-first order, observes at most six light-DOM ancestor
      levels and at most 300 descendant elements per ancestor, fails closed on an unresolved
      truncated narrower subtree, and requires currency-like signal plus BUY_LIKE/CART_LIKE
      structurally paired to a visible native/role button. QUANTITY_LIKE, VARIANT_LIKE and
      pointer-only interaction do not independently satisfy quorum.
    - >
      Generation-7 SUCCESS is valid for any root kind allowed by published V4. TASK-247 must not
      require TITLE_LOCAL_COMMERCE_QUORUM, selected ancestor level 2 or any specific root kind.
      Root selection means only bounded diagnostic root contract satisfied.
    - >
      Non-consuming generation-7 preflight is separate from consuming invocation and may only
      establish: CDP 127.0.0.1:9222 reachability; read-only target enumeration reports exactly one
      normal type=page target total and that sole target is the exact selected PDP/source ID; a
      fresh explicit external generation-7 job root contains none of
      tiktok-pdp-dom-diagnostic-v1.json, tiktok-pdp-dom-diagnostic-v2.json,
      tiktok-pdp-dom-diagnostic-v3.json or tiktok-pdp-dom-diagnostic-v4.json; and all five
      execution-critical files are content-equivalent to generation_7_execution_source_sha
      60da55d5241c7b4c433d9b2d7d5d3725556f443c:
      src/product_intelligence/tiktok_pdp_dom_diagnostic.py,
      src/product_intelligence/cli.py,
      src/product_intelligence/adapters/tiktok_parsing.py,
      src/integrations/playwright/manager.py and
      src/integrations/playwright/session.py. Repository HEAD equality is not required. Preflight
      must not invoke the carrier and does not consume the attempt.
    - >
      The exact fresh external generation-7 job root that passes preflight is bound to the
      subsequent consuming invocation. If that root or browser target state is intentionally
      changed before invocation, the non-consuming preflight must be repeated; repeating preflight
      without carrier invocation does not consume the generation-7 attempt.
    - >
      Human-facing generation-7 invocation transport is exactly one physical PowerShell command
      line with no backtick or multiline continuation. Shell/parser failures before a valid
      tiktok-pdp-dom-diagnostic subcommand dispatches into the carrier are transport failures and
      do not by themselves consume the attempt. Once valid carrier invocation begins, every
      terminal outcome consumes the sole generation-7 attempt.
    - >
      SUCCESS, NO_BOUNDED_PDP_ROOT, BLOCKED_OR_CHALLENGE, LOGIN_GATE, LISTING_UNAVAILABLE,
      IDENTITY_MISMATCH, MALFORMED_DIAGNOSTIC_PAYLOAD, evaluation/session failure or any other
      terminal outcome after valid carrier invocation consumes the sole attempt. No automatic retry,
      refresh, resume, second invocation or replacement target is authorized.
    - >
      SUCCESS may create the bounded V4 artifact. NO_BOUNDED_PDP_ROOT retains the create-exclusive
      V4 FAIL_CLOSED artifact contract. Other safe failures may remain artifact-free according to
      the published carrier. Every artifact retains evidence_authority NONE.
    - >
      After any generation-7 terminal outcome, mandatory review authority returns to
      HUMAN_BRAIN_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_REVIEW. Brain review
      must use actual terminal output plus the exact V4 artifact if created; if an artifact exists,
      its actual SHA256/size/timestamp are review evidence and must not be guessed.
    - >
      Preserve epistemic boundaries after the attempt: V4 SUCCESS does not prove canonical price,
      canonical action capability, Product Truth, acquisition success or collector readiness;
      NO_BOUNDED_PDP_ROOT does not self-authorize repair. Root/topology/quorum observations remain
      engineering diagnostic hints only.
    - >
      Preserve selector_repair_complete false, live_public_pdp_acquisition_authority NONE,
      automated_public_pdp_acquisition_authority NONE, market_test_or_action_authority NONE and
      automatic_progression false. TASK-247 selects no engineering successor.
    - >
      Convert the existing TASK-246 governance regression from current-state ownership to
      historical-state ownership. It must continue asserting exact TASK-246 lineage, Gen-6
      reconciliation, V3 artifact provenance, V4 historical contract and its historical
      post-publication handoff, but must not assert TASK-246 as active_track/current_milestone or
      freeze the current global post_p8_planning_handoff.destination. Prefer explicit inequality
      against TASK-246 historical handoff where appropriate.
    - >
      Add a TASK-247 current-state governance regression as the sole owner of current active-track
      and current global handoff assertions. Historical TASK-243/TASK-245/TASK-246 tests must not
      freeze the TASK-247 global destination.
    - >
      Exact post-publication handoff is
      HUMAN_OPERATOR_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_EXECUTION.
      post_attempt_review_authority and mandatory_post_diagnostic_review are
      HUMAN_BRAIN_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_REVIEW.
    - >
      Keep active_track.next_milestone null, post_p8_planning_handoff.next_milestone null,
      pending_commitments empty and post_run_engineering_successor null. Exact TASK-247 publication
      grants only Human execution handoff, not automatic engineering progression.
    - >
      Modify exactly the five authorized paths. Runtime owns deterministic verification. Executor
      must produce one candidate and one ResultPackage and must not publish, execute TikTok/CDP,
      modify production code, authorize acquisition/action, review its own semantic delta, or begin
      a successor operation.

acceptance:
  - id: AC1
    condition: >
      Exact TASK-246 / RUN-246-002 / REVIEW-246-001 publication lineage at
      60da55d5241c7b4c433d9b2d7d5d3725556f443c is recorded as both V4 implementation and
      generation-7 execution source; failed TASK-246 primary candidate remains historical only.
  - id: AC2
    condition: >
      Generations 1-6 remain immutable consumed history, including exact generation-6
      FAIL_CLOSED / NO_BOUNDED_PDP_ROOT and V3 artifact provenance, while current generation 7 has
      one fresh Human-operated attempt, remaining 1, executed false and no inherited terminal
      outcome.
  - id: AC3
    condition: >
      Exact reviewed TASK-247 publication alone grants one-shot exact-listing generation-7
      live diagnostic authority; arbitrary/replacement/inferred/search/batch/variant/acquisition/
      selector-repair/collector-repair/market-action authorities remain absent.
  - id: AC4
    condition: >
      Schema-V4 root precedence, title-local bounds, truncation fail-closed behavior, strong
      BUY/CART-to-control pairing, supporting-hint exclusions, sanitization and evidence_authority
      NONE remain unchanged with zero production-code modification and no expected live root kind
      or hard-coded ancestor level.
  - id: AC5
    condition: >
      Non-consuming preflight proves CDP reachability, exactly one normal page and exact PDP,
      fresh V1/V2/V3/V4-free external Gen-7 root and five-file content equivalence to exact
      TASK-246 publication without requiring HEAD equality; the passed job root is bound to
      invocation and changed preflight state requires rerunning non-consuming preflight.
  - id: AC6
    condition: >
      Human invocation is one physical PowerShell line; pre-carrier shell/parser transport failure
      is non-consuming; every terminal outcome after valid carrier invocation consumes the sole
      attempt; no retry/refresh/resume/second invocation or replacement target is authorized.
  - id: AC7
    condition: >
      Mandatory post-attempt Brain review is explicit and uses actual terminal/artifact evidence;
      V4 SUCCESS remains bounded diagnostic-root success only, NO_BOUNDED_PDP_ROOT does not
      self-authorize repair, and Product Truth/acquisition/ranking/approval/action boundaries remain
      closed.
  - id: AC8
    condition: >
      Exactly five authorized paths change; TASK-246 governance assertions are historicalized
      without weakening their exact lineage/artifact/V4-history checks; TASK-247 alone owns current
      active-track/global-handoff assertions; focused governance tests and git diff --check pass
      with zero unresolved items; next_milestone remains null, pending commitments empty,
      automatic progression false and exact handoff is
      HUMAN_OPERATOR_GENERATION_7_BOUNDED_TITLE_LOCAL_COMMERCE_ROOT_DIAGNOSTIC_EXECUTION.

verification:
  required:
    - python -m pytest tests/aios_bridge/test_aios_governance_state.py -q --basetemp=.git/aios/pytest-task247-primary
    - git diff --check
